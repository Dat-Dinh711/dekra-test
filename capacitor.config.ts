import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dekra.msk.iqr',
  appName: 'dekra-test',
  webDir: 'dist/dekra-test',
  bundledWebRuntime: false,
};

export default config;
